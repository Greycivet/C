#define _CRT_SECURE_NO_WARNINGS 1

#include <stdio.h>
#include <string.h>
int main() {
	char str[20] = {""};
	printf("是否要好好学习(要/不要)>:");
	scanf("%s",str);
	printf(str);
	if (strcmp(str,"要") == 0)//分支语句if-else
		printf("加油\n");
	else
		printf("不行,得要好好学习\n");

	int line = 0;
	while (line <= 100) { //循环语句
		line++;
		printf("好好学习!\n");
	}

	printf("%d\n", sum(10, 20));
	return 0;
}
int sum(int num1, int num2) { //自定义函数
	return num1 + num2;
}
